// Crie uma função que receba 3 parâmetros (bairro, cidade, estado) e imprima os dados em um 
// formato de endereço, utilizando Template String.

// TEMPLATE STRING

const endereco = (logradouro, cidade, estado) => {
    console.log(`Logradouro: ${logradouro}, cidade: ${cidade}, estado: ${estado}`);
}

let area = endereco("Canto da Lagoa", "Florianópolis", "Santa Catarina");